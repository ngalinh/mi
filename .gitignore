node_modules/
.env
*.log

# SQLite database & WAL files
data/
*.sqlite
*.sqlite-*

# Playwright persistent sessions (chứa cookie đăng nhập Salework/Zalo — KHÔNG commit)
playwright-data/
screenshots/

# Danh sách tài khoản Zalo + lịch sử đăng nhập (tạo lúc chạy) — runtime data, KHÔNG commit
config/zalo-accounts.json
config/login-history.json
# Override chế độ TEST an toàn (chỉnh qua UI) — runtime data, KHÔNG commit
config/test-mode.json

# Cấu hình Claude Code cá nhân (máy-specific)
.claude/settings.local.json

# OS
Thumbs.db
.DS_Store
